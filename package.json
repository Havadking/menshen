{
  "name": "miwifi-monitor",
  "version": "0.1.0",
  "description": "Redmi AX6000 局域网设备在线状态监控：轮询 → 差量/消抖 → SQLite 持久化 → WebSocket 看板",
  "type": "module",
  "private": true,
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "probe": "node scripts/probe.js",
    "test": "node --test \"test/**/*.test.js\""
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "better-sqlite3": "^11.6.0",
    "ws": "^8.18.0"
  }
}
